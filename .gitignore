# Backend
backend/venv/
backend/__pycache__/
backend/**/__pycache__/
backend/.env
backend/*.db

# Raw training datasets (large, downloadable via training scripts)
backend/app/ml/data/

# Trained model artifacts ARE committed so the app runs without retraining.
# (url_only_model.joblib ~42MB, text_model + vectorizer, TLD probs, manifests)

# Frontend
frontend/node_modules/
frontend/dist/

# OS/editor
.DS_Store
*.log
